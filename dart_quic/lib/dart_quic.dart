/// Pure-Dart QUIC v1 client, scoped to leaf commander's needs. See
/// `DESIGN.md` for scope and architecture; the public API surface here
/// grows as milestones land (currently only low-level building blocks).
library;
